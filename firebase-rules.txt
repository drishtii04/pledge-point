// Firebase Security Rules for testing
// Copy and paste these rules into your Firebase Console > Firestore Database > Rules

// TEMPORARY RULES FOR TESTING - ALLOWS ALL READS/WRITES
// WARNING: These rules are for development only!
// Replace with proper authentication rules in production

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

// PRODUCTION RULES (use after testing)
/*
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Volunteers collection - allow writes, restrict reads to admins
    match /volunteers/{docId} {
      allow write: if true; // Allow anyone to submit volunteer applications
      allow read: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Contacts collection - allow writes, restrict reads to admins  
    match /contacts/{docId} {
      allow write: if true; // Allow anyone to submit contact forms
      allow read: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Donations collection - allow writes, restrict reads to admins
    match /donations/{docId} {
      allow write: if true; // Allow anyone to submit donations
      allow read: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Test collection for debugging
    match /test/{docId} {
      allow read, write: if true;
    }
    
    match /connection-test/{docId} {
      allow read, write: if true;
    }
  }
}
*/

// STEPS TO UPDATE FIREBASE RULES:
// 1. Go to Firebase Console (https://console.firebase.google.com)
// 2. Select your project: basava-6e637
// 3. Go to Firestore Database
// 4. Click on "Rules" tab
// 5. Replace the existing rules with the TEMPORARY rules above
// 6. Click "Publish"
// 7. Test your application
// 8. Once working, replace with PRODUCTION rules above